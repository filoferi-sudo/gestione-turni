# Lascia vuota in locale: il proxy di Vite inoltra /api verso http://localhost:4000 (vedi vite.config.js).
# In produzione imposta l'URL pubblico del backend, SENZA slash finale e SENZA /api:
# VITE_API_URL=https://tuo-backend.vercel.app
VITE_API_URL=
