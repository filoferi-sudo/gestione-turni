# Lascia vuoto in locale (usa il proxy di Vite verso /api, vedi vite.config.js).
# In produzione, se il backend è deployato come progetto Vercel separato, imposta l'URL completo:
# VITE_API_BASE_URL=https://tuo-backend.vercel.app
VITE_API_BASE_URL=
