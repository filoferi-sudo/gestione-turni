{
  "name": "turni-app",
  "version": "1.0.0",
  "private": true,
  "description": "Gestione turni - deploy root. Le dipendenze qui replicano quelle di backend/package.json: servono a Vercel per il bundling della funzione serverless in api/.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}
